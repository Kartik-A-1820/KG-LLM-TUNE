# Open Relation Smoke Fixture

`relation_smoke_v1.jsonl` is a tiny synthetic/public-fact fixture for testing
structured relation extraction mechanics. It is not a gate set, not a gold set,
and not part of the private corpus.

Use it to answer one narrow question: given a passage, can the current model emit
the project-style JSON shape and recover simple relation triples with
human-readable relation labels?
