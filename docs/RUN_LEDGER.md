# Run Ledger

This is the factual index of committed runs. Every metric below cites the run
file that contains it. If a value is not in a committed run file, it does not
belong here.

## Ledger

| Stage | Run | Model | Data | Hardware | Status | Recorded metrics | Decision / next step | Caveats |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Open-data local LoRA smoke | `runs/20260918-080000-smollm2-docred-lora-smoke/` | `HuggingFaceTB/SmolLM2-360M-Instruct` | `thunlp/docred`, MIT; source hashes in `runs/20260918-080000-smollm2-docred-lora-smoke/metrics.json` | NVIDIA GeForce GTX 1650 Ti, CUDA available, torch `2.11.0+cu128` in `runs/20260918-080000-smollm2-docred-lora-smoke/env.json` | complete | `train_examples` 16, `val_examples` 4, `initial_val_loss` 0.8313882797956467, `final_val_loss` 0.7843165993690491, `val_loss_delta` -0.047071680426597595, `elapsed_seconds` 94.892, `cuda_max_memory_allocated_bytes` 1076456448, all from `runs/20260918-080000-smollm2-docred-lora-smoke/metrics.json` | Local path, venv, CUDA, tokenizer/model loading, LoRA adapter path, and loss movement are verified for a tiny diagnostic run. Next: build the GraphRAG-specific mix and gold/eval path before any gate claim. | Diagnostic only, not a gate metric. DocRED is format bootstrap data, not the target SFT mix. Run was made from a dirty tree and therefore is provisional per `runs/20260918-080000-smollm2-docred-lora-smoke/env.json`. It predates `docs/BENCHMARKING_PROTOCOL.md`, so no `log.txt` exists. |
| Local QLoRA rank sweep, r=8 | `runs/20260918-203600-smollm2-docred-qlora-r8/` | `HuggingFaceTB/SmolLM2-360M-Instruct` | `thunlp/docred`, MIT; converted train/val hashes in `runs/20260918-203600-smollm2-docred-qlora-r8/metrics.json` | NVIDIA GeForce GTX 1650 Ti, CUDA available, bitsandbytes `0.50.2`, torch `2.11.0+cu128` in `runs/20260918-203600-smollm2-docred-qlora-r8/env.json` | complete | `train_examples` 16, `val_examples` 4, `lora_r` 8, `lora_alpha` 16, `trainable_parameter_count` 4341760, `initial_val_loss` 0.8445938229560852, `final_val_loss` 0.7994894832372665, `val_loss_delta` -0.045104339718818665, `elapsed_seconds` 91.059, `train_input_tokens_per_second_wall` 134.945, `cuda_max_memory_allocated_bytes` 808130560, `cuda_max_memory_reserved_bytes` 878706688, all from `runs/20260918-203600-smollm2-docred-qlora-r8/metrics.json` | Valid QLoRA baseline rank for the sweep. | Diagnostic only, not a gate metric. DocRED is format bootstrap data, not the target SFT mix. Run was made from a dirty tree because the QLoRA trainer changes were under test; treat as provisional until rerun from a clean commit. |
| Local QLoRA rank sweep, r=16 | `runs/20260918-203700-smollm2-docred-qlora-r16/` | `HuggingFaceTB/SmolLM2-360M-Instruct` | `thunlp/docred`, MIT; converted train/val hashes in `runs/20260918-203700-smollm2-docred-qlora-r16/metrics.json` | NVIDIA GeForce GTX 1650 Ti, CUDA available, bitsandbytes `0.50.2`, torch `2.11.0+cu128` in `runs/20260918-203700-smollm2-docred-qlora-r16/env.json` | complete | `train_examples` 16, `val_examples` 4, `lora_r` 16, `lora_alpha` 32, `trainable_parameter_count` 8683520, `initial_val_loss` 0.8445938229560852, `final_val_loss` 0.7679710239171982, `val_loss_delta` -0.07662279903888702, `elapsed_seconds` 91.418, `train_input_tokens_per_second_wall` 134.415, `cuda_max_memory_allocated_bytes` 877598720, `cuda_max_memory_reserved_bytes` 966787072, all from `runs/20260918-203700-smollm2-docred-qlora-r16/metrics.json` | Rank 16 improves loss movement over rank 8 while staying within the same local memory envelope. | Diagnostic only, not a gate metric. DocRED is format bootstrap data, not the target SFT mix. Run was made from a dirty tree because the QLoRA trainer changes were under test; treat as provisional until rerun from a clean commit. |
| Local QLoRA rank sweep, r=32 | `runs/20260918-203800-smollm2-docred-qlora-r32/` | `HuggingFaceTB/SmolLM2-360M-Instruct` | `thunlp/docred`, MIT; converted train/val hashes in `runs/20260918-203800-smollm2-docred-qlora-r32/metrics.json` | NVIDIA GeForce GTX 1650 Ti, CUDA available, bitsandbytes `0.50.2`, torch `2.11.0+cu128` in `runs/20260918-203800-smollm2-docred-qlora-r32/env.json` | complete | `train_examples` 16, `val_examples` 4, `lora_r` 32, `lora_alpha` 64, `trainable_parameter_count` 17367040, `initial_val_loss` 0.8445938229560852, `final_val_loss` 0.7093390077352524, `val_loss_delta` -0.13525481522083282, `elapsed_seconds` 92.238, `train_input_tokens_per_second_wall` 133.22, `cuda_max_memory_allocated_bytes` 1018147328, `cuda_max_memory_reserved_bytes` 1140850688, all from `runs/20260918-203800-smollm2-docred-qlora-r32/metrics.json` | Superseded by the 5k sweep below. | Diagnostic only, not a gate metric. DocRED is format bootstrap data, not the target SFT mix. Run was made from a dirty tree because the QLoRA trainer changes were under test; treat as provisional. |
| Local QLoRA 5k rank sweep, r=8 | `runs/20260918-210000-smollm2-docred5k-qlora-r8/` | `HuggingFaceTB/SmolLM2-360M-Instruct` | `thunlp/docred-train_distant-5k`, MIT; converted train/val hashes in `runs/20260918-210000-smollm2-docred5k-qlora-r8/metrics.json` | NVIDIA GeForce GTX 1650 Ti, CUDA available, bitsandbytes `0.50.2`, torch `2.11.0+cu128` in `runs/20260918-210000-smollm2-docred5k-qlora-r8/env.json` | complete | `train_examples` 5000, `val_examples` 500, `lora_r` 8, `lora_alpha` 16, `optimizer_steps` 625, `trainable_parameter_count` 4341760, `initial_val_loss` 0.8626067816019058, `final_val_loss` 0.3484197201728821, `val_loss_delta` -0.5141870614290238, `losses[0].train_loss` 0.378684344547987, `train_loss_finite` true, `elapsed_seconds` 13566.953, `train_input_tokens_per_second_wall` 141.52, `cuda_max_memory_allocated_bytes` 808130560, `cuda_max_memory_reserved_bytes` 878706688, all from `runs/20260918-210000-smollm2-docred5k-qlora-r8/metrics.json` | Viable low-memory rank. Use it if maximum VRAM margin is the priority. | Diagnostic only, not a gate metric. Public DocRED distant supervision is format bootstrap data, not the target GraphRAG SFT mix. `env.json` records a dirty tree because this trainer version captured the environment after creating its own run directory; future runs are fixed to capture environment before run-directory writes. |
| Local QLoRA 5k rank sweep, r=16 | `runs/20260919-003400-smollm2-docred5k-qlora-r16/` | `HuggingFaceTB/SmolLM2-360M-Instruct` | `thunlp/docred-train_distant-5k`, MIT; converted train/val hashes in `runs/20260919-003400-smollm2-docred5k-qlora-r16/metrics.json` | NVIDIA GeForce GTX 1650 Ti, CUDA available, bitsandbytes `0.50.2`, torch `2.11.0+cu128` in `runs/20260919-003400-smollm2-docred5k-qlora-r16/env.json` | complete | `train_examples` 5000, `val_examples` 500, `lora_r` 16, `lora_alpha` 32, `optimizer_steps` 625, `trainable_parameter_count` 8683520, `initial_val_loss` 0.8626067816019058, `final_val_loss` 0.34637777277827264, `val_loss_delta` -0.5162290088236332, `losses[0].train_loss` 0.3685405244544148, `train_loss_finite` true, `elapsed_seconds` 13614.379, `train_input_tokens_per_second_wall` 141.027, `cuda_max_memory_allocated_bytes` 877598720, `cuda_max_memory_reserved_bytes` 966787072, all from `runs/20260919-003400-smollm2-docred5k-qlora-r16/metrics.json` | Selected rank for the next local GraphRAG-specific QLoRA pilot under this config: it improves validation loss over r=8 with finite training loss and only a modest memory increase. | Diagnostic only, not a gate metric. Public DocRED distant supervision is format bootstrap data, not the target GraphRAG SFT mix. `env.json` records a dirty tree because this trainer version captured the environment after creating its own run directory; future runs are fixed to capture environment before run-directory writes. |
| Local QLoRA 5k rank sweep, r=32 | `runs/20260919-042300-smollm2-docred5k-qlora-r32/` | `HuggingFaceTB/SmolLM2-360M-Instruct` | `thunlp/docred-train_distant-5k`, MIT; converted train/val hashes in `runs/20260919-042300-smollm2-docred5k-qlora-r32/metrics.json` | NVIDIA GeForce GTX 1650 Ti, CUDA available, bitsandbytes `0.50.2`, torch `2.11.0+cu128` in `runs/20260919-042300-smollm2-docred5k-qlora-r32/env.json` | complete | `train_examples` 5000, `val_examples` 500, `lora_r` 32, `lora_alpha` 64, `optimizer_steps` 625, `trainable_parameter_count` 17367040, `initial_val_loss` 0.8626067816019058, `final_val_loss` 0.3438402066230774, `val_loss_delta` -0.5187665749788284, `losses[0].train_loss` null, `train_loss_finite` false, `elapsed_seconds` 13750.881, `train_input_tokens_per_second_wall` 139.627, `cuda_max_memory_allocated_bytes` 1018147328, `cuda_max_memory_reserved_bytes` 1140850688, all from `runs/20260919-042300-smollm2-docred5k-qlora-r32/metrics.json` | Not selected under this config despite the lowest validation loss, because the run recorded non-finite training loss. Retry only with stability mitigations such as a lower learning rate. | Diagnostic only, not a gate metric. Public DocRED distant supervision is format bootstrap data, not the target GraphRAG SFT mix. `env.json` records a dirty tree because this trainer version captured the environment after creating its own run directory; future runs are fixed to capture environment before run-directory writes. |
| Local native-context QLoRA memory probe, r=16 | `runs/20260919-ctx8192-smollm2-qlora-r16-probe/` | `HuggingFaceTB/SmolLM2-360M-Instruct` | Synthetic 8192-token batch; no dataset | NVIDIA GeForce GTX 1650 Ti, CUDA available, bitsandbytes `0.50.2`, torch `2.11.0+cu128` in `runs/20260919-ctx8192-smollm2-qlora-r16-probe/env.json` | failed | `native_max_position_embeddings` 8192, `max_length` 8192, `batch_size` 1, `training_mode` `qlora`, `lora_r` 16, `lora_alpha` 32, `attn_implementation_active` `sdpa`, `failure_type` `OutOfMemoryError`, `elapsed_seconds` 197.242, `cuda_max_memory_allocated_bytes` 10976352768, `cuda_max_memory_reserved_bytes` 11135877120, all from `runs/20260919-ctx8192-smollm2-qlora-r16-probe/metrics.json` | Native-context local QLoRA training at r=16 does not fit on the 4 GB card under this setup. Run real 8192-token GraphRAG capability pilots on Kaggle, or label shorter local runs as smoke-only. | Synthetic memory probe only, not a quality or gate metric. The failure happened during the full-length training step with `sdpa` active. |
| Local context QLoRA memory probe, r=16, 4096 tokens | `runs/20260919-ctx4096-smollm2-qlora-r16-probe/` | `HuggingFaceTB/SmolLM2-360M-Instruct` | Synthetic 4096-token batch; no dataset | NVIDIA GeForce GTX 1650 Ti, CUDA available, bitsandbytes `0.50.2`, torch `2.11.0+cu128` in `runs/20260919-ctx4096-smollm2-qlora-r16-probe/env.json` | complete | `native_max_position_embeddings` 8192, `max_length` 4096, `batch_size` 1, `training_mode` `qlora`, `lora_r` 16, `lora_alpha` 32, `attn_implementation_active` `sdpa`, `loss` 13.170065879821777, `elapsed_seconds` 70.72, `cuda_max_memory_allocated_bytes` 5071700480, `cuda_max_memory_reserved_bytes` 6268387328, all from `runs/20260919-ctx4096-smollm2-qlora-r16-probe/metrics.json` | 4096-token local QLoRA smoke probes fit under this synthetic setup. | Synthetic memory probe only, not a quality or gate metric. `cuda_mem_free_bytes_at_end` is 0, so this is not comfortable headroom. |
| Local context QLoRA memory probe, r=16, 6144 tokens | `runs/20260919-ctx6144-smollm2-qlora-r16-probe/` | `HuggingFaceTB/SmolLM2-360M-Instruct` | Synthetic 6144-token batch; no dataset | NVIDIA GeForce GTX 1650 Ti, CUDA available, bitsandbytes `0.50.2`, torch `2.11.0+cu128` in `runs/20260919-ctx6144-smollm2-qlora-r16-probe/env.json` | complete | `native_max_position_embeddings` 8192, `max_length` 6144, `batch_size` 1, `training_mode` `qlora`, `lora_r` 16, `lora_alpha` 32, `attn_implementation_active` `sdpa`, `loss` 13.170065879821777, `elapsed_seconds` 428.494, `cuda_max_memory_allocated_bytes` 10418790912, `cuda_max_memory_reserved_bytes` 11081351168, all from `runs/20260919-ctx6144-smollm2-qlora-r16-probe/metrics.json` | 6144-token local QLoRA smoke probes fit under this synthetic setup but are slow enough to be stress tests, not efficient pilots. Native 8192-token capability work still belongs on Kaggle. | Synthetic memory probe only, not a quality or gate metric. `cuda_mem_free_bytes_at_end` is 0, so this is not comfortable headroom. |

## Current Evidence State

- There are ten committed local GPU training records: one plain LoRA smoke, one tiny QLoRA rank sweep with r=8, r=16, and r=32, one 5k-example QLoRA rank sweep with r=8, r=16, and r=32, and three context memory probes.
- The current local QLoRA rank choice for short-context smoke or pilot runs is r=16, based on `runs/20260919-003400-smollm2-docred5k-qlora-r16/metrics.json`.
- Native-context local QLoRA training at r=16 failed on the 4 GB card with `OutOfMemoryError`, based on `runs/20260919-ctx8192-smollm2-qlora-r16-probe/metrics.json`; 6144-token synthetic probing completed in `runs/20260919-ctx6144-smollm2-qlora-r16-probe/metrics.json`, but with no free VRAM at end and high elapsed time. Use Kaggle for 8192-token capability pilots.
- There are no Gate 0, Gate 1, Gate 2, or Gate 3 metrics yet.
- There is no committed gold set yet.
- There is no committed Qwen3-0.6B side-by-side pilot yet.
- There is no committed full fine-tune memory test for SmolLM2-360M yet.
